declare module '@govuk-one-login/service-header' {
  /**
   * Enables the header's dropdown behaviour at small screen sizes. The header
   * renders and works without it — the menus simply stay open and the toggle
   * buttons stay hidden.
   */
  export function initCrossServiceHeader(): void
}
